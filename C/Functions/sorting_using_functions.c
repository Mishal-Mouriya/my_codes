




//incomplete


#include <stdio.h>


void sort(int arr[],int n);

int main()
{
    int i,n,sum;
    //intializing array

    printf("enter the no of terms in the array\n");
    scanf("%d",&n);
    int arr[n];
    printf("Enter the values of the array\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }

    sort(arr,n);


     //printing the values of array to check if it is sorted

    printf("Now the elements of array are>\n");
    for(i=0;i<n;i++)
    {
        printf("%d\t",arr[i]);
    }

}

void sort(int arr[],int n)
{
    int swap=1,i;
    while(swap!=0)
    {
        for(i=1;i<n;i++)
        {
            if(arr)
        }

    }
}


